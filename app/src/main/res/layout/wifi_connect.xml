<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="#d0d0d0">

    <ImageView
        android:id="@+id/device_icon"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="65dp"
        app:srcCompat="@drawable/speaker" />

    <TextView
        android:id="@+id/device_control"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="40dp"
        android:layout_marginEnd="40dp"
        android:layout_below="@+id/device_icon"
        android:layout_marginTop="23dp"
        android:text="         按音箱上的控制键3s，直到听到滴滴滴的声音后释放该按键，然后点击连接。"
        android:textSize="20sp"/>

    <Button
        android:id="@+id/wifi_device_connect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="100dp"
        android:background="@drawable/btn_show_next"
        android:layout_alignStart="@id/device_control"
        android:layout_marginEnd="40dp"
        android:text="连           接"
        android:textSize="22sp"
        android:textColor="#000000"/>
</RelativeLayout>